# Copy to .env for local development. Vite exposes only VITE_-prefixed vars.

# Reserved for the API client (upcoming auth phase). In production builds the
# nginx container proxies /api to the API service, so this stays unset.
VITE_API_BASE_URL=http://localhost:8080
